/**
 * @fixme {https://github.com/nikeokoronkwo/doclint} Function adds two rather than one
 */
function _addOne(a: number): number {
    a += 2
    return a
}

/**
 * @todo TA101 - Work on myself
 */
export const _takeMeAway = 0;